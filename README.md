## 🏗️ Архитектура Модели данных

**Users** - пользователи системы с ролевой системой

**Роли:**
- `admin` - полные права администратора
- `manager` - доступ к управлению пользователями  
- `user` - базовые права на свои данные

## 🛠️ Технологии
- **FastAPI** - async web framework
- **PostgreSQL** - база данных
- **SQLAlchemy 2.0** - ORM с async поддержкой
- **JWT** - JSON Web Tokens
- **Argon2** - хеширование паролей через pwdlib
- **Docker** - контейнеризация
- **Alembic** - миграции базы данных

## 🔧 Управление приложением

**Остановка приложения**
```bash
docker-compose down
```

**Полная пересборка (чистый запуск)**
```bash
docker-compose down
docker volume rm auth_system_postgres_data
docker-compose up --build
```

**Просмотр логов**
```bash
docker-compose logs web
docker-compose logs db
```

**Создание новых миграций**
```bash
docker-compose run web alembic revision --autogenerate -m "description"
docker-compose run web alembic upgrade head
```


### 🚀 Первый запуск (для новых разработчиков)

1. **Клонируйте репозиторий по SSH:**
```bash
git clone git@github.com:ScR1pp1/auth_system.git
cd auth_system

Скопируйте файл окружения из примера:
Для MacOS:
cp .env.example .env 
Для Windows:
copy .env.example .env 


При необходимости отредактируйте файл .env (опционально):
nano .env  # или используйте ваш любимый редактор

Запустите приложение:
bash
docker-compose up --build

## 🎯 Особенности системы
- ✅ Современный стек - FastAPI, SQLAlchemy 2.0, Pydantic v2
- ✅ Безопасность - Argon2 для паролей, JWT токены
- ✅ Ролевая система - гибкое управление доступом
- ✅ Тестовые данные - автоматическое создание при первом запуске
- ✅ Async/await - высокая производительность
- ✅ Docker контейнеризация - легкий деплой
- ✅ Автоматическая документация - Swagger UI
- ✅ Миграции базы данных - Alembic

## 💡 Примечания
- При первом запуске автоматически создаются все необходимые таблицы и тестовые пользователи
- Система использует HTTP-only cookies для хранения JWT токенов
- Все пароли хешируются с использованием Argon2
- Тестовые данные создаются только если их еще нет в базе
- Готова к использованию в продакшене

## 🚀 Запуск системы
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up
```

## 👥 Тестовые пользователи

После первого запуска автоматически создаются тестовые пользователи:
- **✅ Тестовые пользователи созданы:**
- **👑 Admin: admin@example.com / admin123**
- **📊 Manager: manager@example.com / manager123**
- **👤 User: user@example.com / user123**


### Роли и возможности:
- **Admin** (`admin@example.com`) - полный доступ ко всем функциям системы
- **Manager** (`manager@example.com`) - доступ к управлению пользователями
- **User** (`user@example.com`) - базовые права на свои данные